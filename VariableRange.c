/* MKC */

#include <stdio.h>     
#include <limits.h>    
#include <stdlib.h>
#include <float.h>

int main () {

  printf( "Minimum value for int: %d \n", INT_MIN);
  printf( "Minimum value for int: %d \n", INT_MAX);
  printf( "Minimum value for float: %e \n", FLT_MIN);
  printf( "Minimum value for float: %e \n", FLT_MAX);
  printf( "Minimum value for double: %e \n", DBL_MIN);
  printf( "Minimum value for double: %e \n", DBL_MAX);
  system("PAUSE");
  return 0;
}
